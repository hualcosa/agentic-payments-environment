# Benchmark v0 — claude-haiku-4-5 — prompt v1

**not yet measured**

No API credentials were available to run `apenv bench --agent llm` for this
model. This file is a placeholder so the README can link a committed report
without inventing `safe_success_rate`, task-success, or catastrophic counts.

When a real run is reviewed, replace this text with the `report.md` layout
from docs/08 §5 (headline rates, per-code catastrophic counts, per-task
table) generated from `runs/` artifacts. Until then, do not cite numeric
results from this file.
