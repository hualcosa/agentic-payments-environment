# Catastrophic-code example traces

T1.06 LLM baseline reports are **not yet measured** (no provider API
credentials in the producing environment). There are therefore no observed
catastrophic codes from a reviewed model run to annotate.

When the first real `apenv bench --agent llm` report is committed, add one
annotated example per observed catastrophic code here (markdown or JSON
pointing at a `runs/` trace plus a short note on the failure mode). Until
then this directory holds no episode examples.
