# Benchmark v0.1 decision

The M2 scripted annotation corpus and taxonomy counts did not surface a
task-spec ambiguity that would require splitting or rewriting a frozen v0
task.

**No v0.1 freeze.** `benchmarks/v0/*.json` remains the evaluated set.
Do not edit those files in place. If a later milestone needs a corrected
task, add `benchmarks/v0.1/` as a new freeze and keep v0 on disk.
