# Handoffs

Handoffs preserve continuation context between sessions and harnesses. Create
them from `../templates/handoff.md` and follow the naming convention in the
canonical coordination protocol.

Handoffs are historical snapshots. Update the active task with any new state
rather than editing an old handoff.
