# Backlog

This directory contains defined, unclaimed tasks. Create tasks from
`../../templates/task.md`. Claiming a task means moving its file to
`../in-progress/` and completing its ownership fields.
