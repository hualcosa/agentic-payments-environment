# In-progress tasks

This directory contains claimed tasks. Every task here must identify its
harness, owner label, claim time, scope, and expected files. Check all active
tasks for overlap before editing project files.
